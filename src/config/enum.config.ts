export enum TabAction {
  CLOSE_OTHER,
  CLOSE_RIGHT,
}

export enum DisplayScene {
  list = 'LIST',
  cover = 'COVER',
  normal = 'Normal',
  avatar = 'Avatar',
}

export enum FileType {
  image = 'image',
  video = 'video',
  document = 'document',
  other = 'other',
}
